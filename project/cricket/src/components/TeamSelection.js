import React from "react";
import { Container, Typography } from "@mui/material";

const TeamSelection = () => {
  return (
    <Container>
      <Typography variant="h4">Team Selection</Typography>
      <p>Select players to form your fantasy team.</p>
    </Container>
  );
};

export default TeamSelection;
