import React from "react";
import { Container, Typography } from "@mui/material";

const Players = () => {
  return (
    <Container>
      <Typography variant="h4">Players List</Typography>
      <p>List of available players for team selection.</p>
    </Container>
  );
};

export default Players;
