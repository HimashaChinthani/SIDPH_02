import React from "react";
import { Container, Typography } from "@mui/material";

const Leaderboard = () => {
  return (
    <Container>
      <Typography variant="h4">Leaderboard</Typography>
      <p>View the rankings of top teams.</p>
    </Container>
  );
};

export default Leaderboard;
